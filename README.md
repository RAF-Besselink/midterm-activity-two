REACT JS + JS
*******************
Requirements
*******************
1. NodeJS
2. Git
3. IDE ->  this is if you want to edit the codebase

***********************
How to install and run
***********************
1. Clone or download the app from the repo
2. run the command below in the terminal or cmd
working dir>npm install
3. run the command below to run this app in your local machine
working dir> npm run dev or npm run build